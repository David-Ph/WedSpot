const { Request, Notification, vendor, User } = require("../../models");
const validator = require("validator");

exports.createNotification = async (req, res, next) => {
  try {
  } catch (error) {
    next(error);
  }
};
